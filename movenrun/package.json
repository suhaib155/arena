{
  "name": "movenrun",
  "version": "0.0.1",
  "private": true,
  "workspaces": [
    "shared",
    "contracts",
    "backend",
    "mobile"
  ],
  "scripts": {
    "build": "yarn workspaces foreach --all --exclude movenrun --topological-dev run build",
    "test": "yarn workspaces foreach --all --exclude movenrun run test",
    "lint": "yarn workspaces foreach --all --exclude movenrun run lint",
    "verify:contracts": "yarn workspace @movenrun/contracts compile && yarn workspace @movenrun/contracts test",
    "eas-build-pre-install": "corepack enable"
  },
  "packageManager": "yarn@4.9.1"
}
