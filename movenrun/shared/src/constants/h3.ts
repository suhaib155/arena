export const H3_RESOLUTION = 8;

// Minimum unique movers in a hex over 90 days before minting is allowed
export const MIN_ACTIVITY_THRESHOLD = 5;

// Minimum days of activity data required before minting eligibility check
export const MIN_ACTIVITY_DAYS = 90;

// Hex area at resolution 8 is ~0.74 km²
export const HEX_AREA_KM2 = 0.74;

// Days of inactivity before a zone can be marked dormant
export const DORMANCY_DAYS = 180;

// Days of dormancy before NFT can be burned and hex re-opened
export const RECLAIM_DAYS = 210;
